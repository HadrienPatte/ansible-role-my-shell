---
services: docker

language: python
python:
  - 3.6
cache: pip

install:
  - pip install molecule docker

before_script:
  - cd ..
  - mv ansible-role-my-shell hadrienpatte.my-shell
  - cd hadrienpatte.my-shell

script:
  - molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
...
